<Window
    x:Class="EmailClientPluma.MVVM.Views.EmailLabelEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:EmailClientPluma.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:EmailClientPluma.MVVM.ViewModels"
    Title="EmailLabelEditView"
    Width="800"
    Height="450"
    Background="{DynamicResource BackgroundBrush}"
    mc:Ignorable="d">

    <Window.DataContext>
        <viewmodels:EmailLabelEditViewModel />
    </Window.DataContext>
    <Window.Resources>
        <Style BasedOn="{StaticResource GBTB}" TargetType="TextBlock" />
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter" />
        <Style x:Key="LabelItemContainerStyle" TargetType="Border">
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Background" Value="#08000000" />
        </Style>
    </Window.Resources>
    <Grid Margin="16" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--  Header  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            FontSize="16"
            FontWeight="Bold"
            Text="Edit labels for this email" />
        <!--  Search + New label  -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" TextAlignment="Center">Search</TextBlock>
            <TextBox
                Grid.Column="1"
                Height="28"
                Margin="10,0,0,0"
                VerticalContentAlignment="Center"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                ToolTip="Filter labels by name" />

        </Grid>
        <!--  Labels list  -->
        <Border
            Grid.Row="2"
            BorderBrush="{DynamicResource TextBrush}"
            BorderThickness="1"
            CornerRadius="4">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding FilteredLabels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource LabelItemContainerStyle}">
                                <DockPanel>
                                    <!--  Checkbox for multi-tagging  -->
                                    <CheckBox
                                        Margin="0,0,6,0"
                                        VerticalAlignment="Center"
                                        IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                        IsEnabled="{Binding IsModifiable}" />
                                    <!--  Optional color indicator (if you have LabelColor)  -->
                                    <Rectangle
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,6,0"
                                        VerticalAlignment="Center"
                                        RadiusX="2"
                                        RadiusY="2" />
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock
                                            FontWeight="SemiBold"
                                            Foreground="{Binding Color, Converter={StaticResource ColorToBrushConverter}, FallbackValue=Transparent}"
                                            Text="{Binding Name}" />
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <!--  Buttons  -->
        <StackPanel
            Grid.Row="3"
            Margin="0,12,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="80"
                Margin="0,0,8,0"
                Command="{Binding SaveCommand}"
                Content="OK"
                IsDefault="True" />
            <Button
                Width="80"
                Command="{Binding CancelCommand}"
                Content="Cancel"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>