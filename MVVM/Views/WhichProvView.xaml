<Window
    x:Class="EmailClientPluma.MVVM.Views.WhichProvView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:EmailClientPluma.MVVM.ViewModels"
    Title="WhichProvView"
    Width="341"
    Height="376"
    mc:Ignorable="d">
    <Window.Resources>
        <Style BasedOn="{StaticResource GBTB}" TargetType="TextBlock" />
    </Window.Resources>

    <Window.DataContext>
        <viewmodels:WhichProvViewModel />
    </Window.DataContext>
    <Grid Background="{DynamicResource BackgroundBrush}">
        <TextBlock
            Margin="0,20,0,0"
            HorizontalAlignment="Center"
            FontSize="24"
            FontWeight="Bold"
            Text="Select Your Email Provider" />
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button
                Width="156"
                Height="40"
                Margin="0,0,0,20"
                Command="{Binding AddAccountGoogleCommand}"
                Content="Google" />
            <Button
                Width="159"
                Height="40"
                Command="{Binding AddAccountMicrosoftCommand}"
                Content="Microsoft" />
        </StackPanel>
    </Grid>
</Window>
