<Window x:Class="EmailClientPluma.MVVM.Views.WhichProvView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:EmailClientPluma.Behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:EmailClientPluma"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:EmailClientPluma.MVVM.ViewModels"
        mc:Ignorable="d"
        Title="WhichProvView" Height="450" Width="800">
    


    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource GBTB}" />
    </Window.Resources>

    <Grid>
        <TextBlock Text="Select Your Email Provider" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Google" Width="100" Height="40" Command="{Binding AddAccountCommand_GG}" Margin="0,0,0,20"/>
            <Button Content="Microsoft" Width="100" Height="40"/>
        </StackPanel>
    </Grid>
</Window>
