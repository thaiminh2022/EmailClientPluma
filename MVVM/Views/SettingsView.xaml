<Window
    x:Class="EmailClientPluma.MVVM.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EmailClientPluma.MVVM.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:EmailClientPluma.MVVM.ViewModels"
    Title="Settings"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <viewmodels:SettingsViewModel />
    </Window.DataContext>

    <Window.Resources>
        <Style BasedOn="{StaticResource GBTB}" TargetType="TextBlock" />
    </Window.Resources>

    <DockPanel Background="{DynamicResource PanelBackgroundBrush}">

        <!--  HEADER  -->
        <Border
            Height="52"
            Padding="10"
            Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource AccentBrush}"
            BorderThickness="0,0,0,1"
            DockPanel.Dock="Top">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Foreground="{DynamicResource TextBrush}"
                Text="Settings" />
        </Border>


        <!--  MAIN TABS  -->
        <TabControl Padding="0" BorderThickness="0">
            <!--  GENERAL  -->
            <TabItem Header="General">
                <StackPanel Margin="20">
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Appearance">
                        <StackPanel Margin="10">
                            <DockPanel>
                                <TextBlock Width="200" Text="Theme" />
                                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                    <Button
                                        x:Name="LightModeBtn"
                                        Width="160"
                                        Click="LightModeBtn_Click"
                                        Content="Light Mode" />
                                    <Button
                                        x:Name="DarkModeBtn"
                                        Width="160"
                                        Margin="0,0,0,0"
                                        Click="DarkModeBtn_Click"
                                        Content="Dark Mode" />
                                </StackPanel>
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Sync Refresh">
                        <StackPanel Margin="10">
                            <DockPanel>
                                <TextBlock Width="200" Text="Auto refresh (seconds)" />
                                <TextBox Width="100" Text="60" />
                            </DockPanel>
                            <CheckBox Content="Background message sync" IsChecked="True" />
                            <CheckBox Content="Increase polling time when idle for too long" IsChecked="True" />
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>

            <!--  ACCOUNTS  -->
            <TabItem Header="Accounts">
                <StackPanel Margin="20">
                    <Button Margin="0,5">Add Account</Button>
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Google Accounts">
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Remove" />
                            </StackPanel>
                            <DockPanel Margin="5">
                                <TextBlock Text="example@gmail.com" />
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Microsoft Accounts">
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Remove" />
                            </StackPanel>
                            <DockPanel Margin="5">
                                <TextBlock Text="example@gmail.com" />
                            </DockPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>

            <!--  SECURITY  -->
            <TabItem Header="Security">
                <StackPanel Margin="20">
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Phishing Protection">
                        <StackPanel Margin="10">
                            <CheckBox Content="Use phishing detector" IsChecked="True" />
                            <CheckBox Content="Use BERT phishing detector" IsChecked="True" />
                        </StackPanel>
                    </GroupBox>


                </StackPanel>
            </TabItem>
            <!--  STORAGE  -->
            <TabItem Header="Storage">
                <StackPanel Margin="20">
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Local database">
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="0">Open data folder</Button>
                            </StackPanel>
                            <Button Margin="0,5">Clear database</Button>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox
                        BorderBrush="{DynamicResource TextBrush}"
                        BorderThickness="1"
                        Header="Log files">
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="0">Open data folder</Button>
                            </StackPanel>
                            <Button Margin="0,5">Clear log files</Button>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
